name: Container SBOM

on:
  push:
    branches:
      - dev
      - main
    paths:
      - Dockerfile
      - package.json
      - package-lock.json
      - pom.xml
      - src/**
      - prisma/**
      - .github/workflows/sbom.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sbom:
    name: Generate user SBOM
    uses: abrashee/bema-infrastructure/.github/workflows/reusable-container-sbom.yml@dev
    with:
      repository: abrashee/bema-user-service
      ref: ${{ github.sha }}
      image-name: bema-user-service
      artifact-name: user
